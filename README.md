# renovate-config

Shared Renovate configuration for all personal projects.

Repos extend this via:
​```
{"extends": ["github>chrismitsdev/renovate-config"]}
​```

Edit `default.json` here to change update rules across all repos at once.
